# Go Style Guide

- Use the latest version of Go unless it is a zero version release (e.g. 1.25.0).
- Use idiomatic Go naming conventions.
- Use Go's built-in types and functions over external libraries whenever possible.
- Use Go's built-in concurrency primitives when necessary.
